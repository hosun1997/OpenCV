/*
	OutputArray 클래스는 InputArray 클래스에서 상속을 받은 클래스에서 상속을 받은 클래스로 OpenCV함수의 출력으로 사용될 벡터나 행렬을 인수로 전달할 때 사용됩니다.
	OutputArray 클래스인 인수는 읽기와 쓰기가 가능하며 Mat, Mat_<T>, Mat<T, m, n>, std::vector<T>, std::vector<std::vector<T>>, std::vector<Mat>, noArray() 등을 전달할 수 있습니다.
*/